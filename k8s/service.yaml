apiVersion: v1
kind: Service
metadata:
  name: matchify-backend-service
spec:
  selector:
    app: matchify-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
